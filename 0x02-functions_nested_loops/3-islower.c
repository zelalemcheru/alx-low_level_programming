#include "main.h"

/**
 * _islower if c is lower case return 1
 * otherwise return 0
 * main - check the code.
 * 
 * Return: 0 if success ful islower -entry point
 */

int _islower(int c);
{
	if (c >= 97; && c <= 122)
		return (1);
	else
		return (0);
}
